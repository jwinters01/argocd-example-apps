apiVersion: promoter.argoproj.io/v1alpha1
kind: PromotionStrategy
metadata:
  name: demo-github
  namespace: helm-guestbook-promotion
spec:
  activeCommitStatuses:
    # The ArgoCDCommitStatus CR will maintain this commit status based on the application health.
    - key: argocd-health
  environments:
    - autoMerge: true
      branch: environment/development
      proposedCommitStatuses:
      - key: integration-tests-dev
    - autoMerge: true
      branch: environment/staging
      proposedCommitStatuses:
      - key: integration-tests-stg
    - autoMerge: false
      branch: environment/prod
      proposedCommitStatuses:
      - key: integration-tests-prod
      - key: hcap
  gitRepositoryRef:
    name: github-argocd-example-apps
